/**
 * The agent object.
 * @property {string} id - The ID of the agent.
 * @property {string} name - The name of the agent.
 * @property {number} orders - The number of orders the agent has.
 */
type Agent = {
  id: string;
  name: string;
  orders: number;
};

export default Agent;
